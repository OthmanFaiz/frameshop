// just a page to render components for while i know which i should built the adreline main page

import Main from '../components/Layout/Main';

export default function PageTest() {
	return (
		<div>
			<Main />
		</div>
	);
}
